# Load bladeRF-wiphy kernel driver for /etc/modprobe.d

# Make availabe as 'bladeRF-wiphy' 
alias bladeRF-wiphy bladeRF_mac80211_hwsim

# Prevent the standard 'bladeRF_mac80211_hwsim' module from being loaded
blacklist mac80211_hwsim

# Load dependencies
# install bladeRF_mac80211_hwsim /sbin/modprobe mac80211 && \
#     /sbin/modprobe cfg80211 && \
#     modprobe --no-install bladeRF_mac80211_hwsim $CMDLINE_OPTS


